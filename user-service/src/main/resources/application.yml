server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres-citizen-report:5432/userdb
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
